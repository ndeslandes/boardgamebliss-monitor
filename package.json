{
  "name": "boardgamebliss-monitor",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently -n next,worker -c cyan,green \"next dev\" \"node worker.js\"",
    "build": "next build",
    "start": "concurrently -n next,worker -c cyan,green \"next start\" \"node worker.js\"",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "next": "^14.2.5",
    "node-cron": "^3.0.3",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/node-cron": "^3.0.11",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10",
    "postcss": "^8",
    "tailwindcss": "^3",
    "typescript": "^5",
    "vitest": "^4.1.5"
  }
}
